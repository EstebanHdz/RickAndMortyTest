const useLocalStorage = () => {

    const getFavourites = () => {
        const favs = localStorage.getItem("favourites")
    }

    return (
        <div>
        
        </div>
    )
}

export default useLocalStorage
